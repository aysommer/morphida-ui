import { ColorScheme, FontColor } from "./types";

export const BACKGROUND_COLORS: Record<ColorScheme, string> = {
   gray: "var(--gray-color)",
   blue: "var(--blue-color)",
   green: "var(--green-color)",
};

export const FONT_COLORS: Record<FontColor, string> = {
   white: "var(--white-color)",
   black: "var(--black-color",
};
